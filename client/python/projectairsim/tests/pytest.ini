[pytest]
junit_family=xunit2
markers =
    offline: no simulator required
    runtime: engine-independent PAS Runtime integration
    unreal: shared-scene Unreal integration and rendering
    px4: PX4 SITL integration tests (require PX4_ROOT and PX4 SITL). Run with: pytest -m px4
addopts = -m "not px4"
